import{PlayerSave,PropType}from'../domain/Types';
declare const wx:undefined|{getStorageSync?:(key:string)=>unknown;setStorageSync?:(key:string,value:unknown)=>void};
const KEY='flower_vase_transfer_save_v3';const props=():Record<PropType,number>=>({hint:0,hourglass:2,magic:1});const fresh=():PlayerSave=>({version:3,unlockedLevel:1,levelStars:{},bestScores:{},coins:0,videoUnlockedLevels:{},props:props(),music:true,sfx:true,vibration:true,tutorialStep:0});
export class SaveService{static load(){try{const raw=typeof wx!=='undefined'?wx.getStorageSync?.(KEY):localStorage.getItem(KEY),p=typeof raw==='string'?JSON.parse(raw):raw||{};return{...fresh(),...p,coins:0,videoUnlockedLevels:{...((p as Partial<PlayerSave>).videoUnlockedLevels||{})},props:{...props(),...(p as Partial<PlayerSave>).props}}as PlayerSave;}catch{return fresh();}}static write(s:PlayerSave){if(typeof wx!=='undefined')wx.setStorageSync?.(KEY,s);else localStorage.setItem(KEY,JSON.stringify(s));}static totalStars(s=this.load()){return Object.keys(s.levelStars).reduce((sum:number,key:string)=>sum+(s.levelStars[key]||0),0);}static requiredStars(id:number){if(id<=1)return 0;const steps=[2,2,2,3,2,2,3,2,3,2],count=id-1,cycles=Math.floor(count/steps.length),rest=count%steps.length;return cycles*steps.reduce((a,b)=>a+b,0)+steps.slice(0,rest).reduce((a,b)=>a+b,0);}static setAudio(kind:'music'|'sfx',enabled:boolean){const s=this.load();s[kind]=enabled;this.write(s);}static unlockByVideo(id:number){const s=this.load();s.videoUnlockedLevels[String(id)]=true;this.write(s);}static markTutorialDone(){const s=this.load();s.tutorialStep=1;this.write(s);}static complete(id:number,stars:number,score=0){const s=this.load(),k=String(id);s.levelStars[k]=Math.max(s.levelStars[k]??0,stars);s.bestScores[k]=Math.max(s.bestScores[k]??0,score);s.unlockedLevel=Math.min(60,Math.max(s.unlockedLevel,id+1));this.write(s);}}
